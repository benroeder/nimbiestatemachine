[pytest]
testpaths = tests
markers =
    hardware: marks tests that require real Nimbie hardware (deselect with '-m "not hardware"')
    slow: marks tests as slow (deselect with '-m "not slow"')
addopts = -v